//
//  ViewController.swift
//  HelloScope
//
//  Created by Sean on 12/11/24.
//

import UIKit

// 1. Global Scope

class ViewController: UIViewController {

     /* 6. Class Scope but kxc; 선언 스코프, 타입 스코프라고 개인적으로 사용한다.
     Local Scope라고 부르지 않는다.
     이유는 문법적으로 선언만 할 수 있기 때문이다. (문법적으로 = 지키지 않으면 에러가 발생한다.)
     class, struct, enum, extension, protocol
     키워드 다음에 처음 오는 braces 안에는
     print 호출 같은 문장은 올 수 없고, 속성, 메소드, 생성자 등의 멤버 선언만 할 수 있다.
      */
    
      
    override func viewDidLoad() {
        
        // 3.
        
        super.viewDidLoad()
        
        // 4.
        
    }

    // 5.

}

// 2. Global Scope
